import 'package:flutter/material.dart';

class HomepageDetail extends StatelessWidget {
  const HomepageDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Homepage detail"));
  }
}
